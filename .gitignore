# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# ...nhưng file mẫu thì PHẢI commit, nếu không người clone về không biết
# cần những biến nào. Dòng .env* ở trên nuốt luôn cả nó nếu thiếu dòng này.
!.env.example

# Ghi chú cá nhân chuẩn bị phỏng vấn — chỉ giữ ở máy, không đẩy lên GitHub
/NOTES.md

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# ảnh nguồn chưa xử lý
# Repo công khai. Bản đã resize dùng trên trang nằm ở public/portrait.jpg và
# vẫn được commit; thư mục này giữ file gốc chưa cắt, không cần lên GitHub.
/image
